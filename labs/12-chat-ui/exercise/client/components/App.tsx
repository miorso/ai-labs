import { useChat } from '@ai-sdk/react';
import { useState } from 'react';
import { Message } from './Message.tsx';
import { Input } from './Input.tsx';

export const App = () => {
  // TODO: Use useChat() hook to get messages and sendMessage
  // TODO: Create input state with useState('')
  // TODO: Return a container div with:
  //   - Map over messages and render Message component for each
  //   - Render Input component with:
  //     - input value
  //     - onChange handler to update input state
  //     - onSubmit handler that calls sendMessage and clears input
};
